import AboutPageSection from '../interfaces/pages/about-section-content';

type Props = {

}

function DarkModeToggle({ }: Props) : JSX.Element {
  return (
    <div>
    
    </div>
  )
}

export default DarkModeToggle;
