import ProjectType from '../interfaces/project'

type Props = {
  project: ProjectType
}

function ProjectDetails({ project }: Props) : JSX.Element {
  return (
    <div>
    
    </div>
  )
}

export default ProjectDetails;
