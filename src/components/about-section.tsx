import AboutPageSection from '../interfaces/pages/about-section-content';

type Props = {
    sectionContent: AboutPageSection
}

function AboutSection({ sectionContent }: Props) : JSX.Element {
  return (
    <div>
    
    </div>
  )
}

export default AboutSection;
